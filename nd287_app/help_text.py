# -*- coding: utf-8 -*-
"""アプリ内「詳細ヘルプ」の本文（HTML）。

gui.HelpDialog が左の見出し一覧＋右の本文として表示する。
HELP_SECTIONS = [(見出し, HTML本文), …]。内容は README と実装に合わせてある。
"""

APP_OVERVIEW = """
<h3>このアプリについて</h3>
<p>HEIDENHAIN <b>ND287</b> から測定値を読み取り、回転テーブルの
<b>割出精度・再現性</b>を計算・判定・記録するアプリです。LabVIEW製の旧
「分割測定」系アプリのPython移植版で、結果は<b>新CSV</b>に加えて
検査表システムが読む<b>旧形式 .BS/.KS（バイト一致）</b>でも保存できます。</p>
<ul>
<li>ND287とは <b>RS-232C</b> で接続（USB直結=X32／変換器経由=X31を画面右下で切替）。</li>
<li>FANUC測定プログラム(Gコード)の生成、生成プログラルの<b>機械へのLAN送信</b>、
    SwitchBotによる自動測定、Webアプリ連携にも対応。</li>
<li>実機が無くても <code>ダミーで起動.bat</code>（または <code>--dummy</code>）で
    画面と操作を確認できます。</li>
</ul>
<p>各画面の詳しい使い方は左の見出しから選んでください。
SwitchBotと「機械へ送信」は、それぞれの設定画面内の専用ヘルプ
（「接続方法」「送信の使い方」）にも手順があります。</p>
"""

MODES = """
<h3>測定モード（画面左上で切替）</h3>
<ul>
<li><b>回転分割</b> … 回転テーブルの割出精度（ホイールCW/CCW・ウォームCW/CCW）。</li>
<li><b>傾斜分割</b> … 傾斜軸の割出精度。バックラッシの合否判定はしない（MIN/MAX表示のみ）。</li>
<li><b>回転再現性 / 傾斜再現性</b> … 指定ブロックを繰り返し測り、ばらつき（最大−最小）を見る。</li>
<li><b>回転分割+再現 / 傾斜分割+再現</b> … 分割を測り終えてから各ブロックで再現をN回。
    受信順はFANUC合体プログラムの完了信号(M80)の発火順と完全一致します。</li>
</ul>
<p>分割は「ホイール（粗い割り）」と「ウォーム（細かい割り）」を別々に測り、
それぞれCW（正転）・CCW（逆転）で読みます。</p>
"""

MEASURE_FLOW = """
<h3>測定の進め方</h3>
<ol>
<li>型式・機番・測定温度などを入力（型式マスタがあれば測定条件が自動設定）。</li>
<li><b>取込開始</b>を押し、各指令角度でND287の値が自動で取り込まれる。
    手動取込・1点戻る・中止は「取込中の操作」から。</li>
<li>必要なら<b>バックラッシ手動補正</b>とコメントを入力。</li>
<li><b>セーブ</b>でCSV＋.BS/.KSを保存、<b>印刷</b>でA4横の検査記録を出力。
<b>「PDF成績書」</b>で同じ内容を検査成績書PDFとして保存できます（印刷と体裁は同じ。
プリンタが無くてもファイルで残せ、メール添付や電子保管に使えます。ISO230モードでは
A/R/E/M/B の規格値も載ります）。</li>
</ol>
<p><b>部分再測定</b>: 一度測ったあと、選んだ系列（例 ホイールCWだけ）を
測り直せます。他の系列のデータはそのまま残ります。</p>
<p><b>「ツール ▾」</b>: 分析／プログラム作成／パラメータ／アラーム はこのメニューに
まとまっています（ツールバーを短くして測定条件とグラフを広く使うため）。</p>
<p><b>自動測定</b>: SwitchBotで機械の起動ボタンを物理的に押し、取込→判定→
NGなら自動で測り直す（下の「SwitchBotで自動測定」参照）。</p>
"""

RESULTS = """
<h3>画面に出る結果</h3>
<p>系列ごと（ホイールCW/CCW・ウォームCW/CCW）に次を表示します。</p>
<ul>
<li><b>精度PP</b> … 偏差の最大−最小。</li>
<li><b>単一誤差</b> … 隣の点へ移ったときの1ステップ誤差の最大。</li>
<li><b>隣接誤差</b> … 連続する2つの単一誤差が逆向きに重なった突起の最大。</li>
<li><b>傾き</b> … 一周して戻るべき位置に戻らなかったぶん（最後−最初の偏差）。</li>
<li><b>主点評価（精度1）</b> … 等分数を入れると主点だけのPPを出す。</li>
</ul>
<p><b>総合バックラッシ MIN/MAX/平均/差</b> … 同一指令角度の「CCW偏差−CW偏差」から
求めた総合値。<u>画面に出すのは総合のみ</u>。回転は測定温度の規格とOK/NGを併記します。</p>
<p><b>（参考）ホイール/ウォーム単品のバックラッシ</b> … 既定は非表示。設定の
「ホイール/ウォーム単品のバックラッシも表示」をONにしたときだけ参考表示します。
真の最大最小・総合判定は<b>画面にも印刷（検査記録）にも出しません</b>
（印刷は画面と同じ内容に揃えてあります）。</p>
"""

ISO230 = """
<h3>JIS B 6190-2（ISO 230-2）評価</h3>
<p>再現性のデータを、工作機械の位置決め精度の標準規格
<b>JIS B 6190-2（＝ISO 230-2）</b>の式で評価します。お客様（機械メーカー）の
受入試験と同じ土俵の数値なので、<b>検査成績書の提出値</b>に使えます。</p>
<p><b>専用の測定モード「位置決め精度(ISO230)」</b>を用意しました。モードで選ぶと、
JISの標準サイクル（<b>8点・各方向5回・0〜315°</b>）が既定で入り、目標位置には
周期誤差と重ならないよう<b>擬似ランダムオフセット</b>（JIS推奨。チェックで切替、
両端は範囲を保つため固定）が付きます。測定を終えると結果表に A/R/E/M/B の主要値が
そのまま並びます。中身は再現性測定と同じ仕組みなので、測定プログラム作成・保存
（.RS）・Web連携も従来どおり使えます。</p>
<p>使い方: 「位置決め精度(ISO230)」（または再現性/分割+再現）の測定後・ロード後に、
グラフ上の<b>「JIS評価」</b>ボタンを押すと位置別の詳細が見られます。<b>CSVで保存</b>
すればExcelで成績書に貼れます。既存の「再現性（最大−最小）」の画面・保存・旧形式
.RS出力は<b>従来のまま変わりません</b>。</p>
<ul>
<li><b>A（双方向位置決め精度）</b> … 提出値の代表。ばらつき(±2s)込みの最悪幅。</li>
<li><b>R（繰返し性）</b> … Ri = 2si↑+2si↓+|Bi| の最大（片方向は Ri=4si）。</li>
<li><b>E（系統誤差）／M（平均双方向誤差）</b> … 平均カーブの幅。</li>
<li><b>B（反転値）</b> … CW/CCWの平均差＝いわゆるバックラッシ相当。B̄は平均。</li>
</ul>
<p>方向は <b>↑=CW、↓=CCW</b>、単位は秒["]。JISの標準サイクルは
<b>各方向5回・目標位置は0/90/180/270°を含む8点以上</b>（±30%のランダムオフセット
推奨）です。回数・点数が少なくても計算はしますが、注意書きで知らせます
（成績書に使うときは5回×8点以上での測定をおすすめします）。</p>
"""

BACKLASH_CORRECTION = """
<h3>バックラッシ手動補正</h3>
<p>実際のメカ的な隙間と測定結果に差があるとき、その差分[秒]を入力すると、
ホイール・ウォーム両方のバックラッシMIN/MAXに加算され、温度別の合否判定も
補正後の値で再計算されます。適用した補正値は結果表に「バックラッシ手動補正」
として表示されます。生データ・偏差グラフには影響しません。</p>
"""

TEMP_JUDGE = """
<h3>温度別の合否判定</h3>
<p>ホイールが合金製で熱膨張するため、規格が測定温度で変わります。判定は
型式マスタ（合否判定CSV）の<b>温度式</b>を優先します:</p>
<p style="margin-left:1em;"><code>MIN = MINA×温度 + MINB ／ MAX = MAXA×温度 + MAXB</code></p>
<p>型式マスタに式が無い場合は、settings.json の <code>judgement_spec</code> の
温度帯別規格を使います。傾斜分割はバックラッシの合否判定をしません。</p>
"""

PAST_DATA = """
<h3>過去データ（.BS/.KS検索）</h3>
<p><b>過去データ</b>ボタンで、.BS/.KS（検査表）フォルダを<b>型式・機番</b>で検索します。
保存先は型式の頭文字フォルダ（例 RWE）に分かれており、型式を指定するとその
フォルダだけを走査するので軽く・速く探せます。</p>
<ul>
<li>表示される値は<b>旧アプリが保存した結果そのまま</b>なので旧アプリと一致します。</li>
<li>行をダブルクリック（または「選択をロード」）でそのデータを本画面に再表示。</li>
<li>「CSV出力」で検索結果一覧を保存。横断比較は「分析」へ。</li>
</ul>
"""

ANALYSIS = """
<h3>分析（横断比較・1件詳細）</h3>
<p><b>分析</b>ボタンで2つのタブを開きます。</p>
<ul>
<li><b>横断比較</b> … .BS/.KSフォルダを型式・機番で検索し、各測定を1行に並べる。
    <b>「種別」</b>（すべて／回転分割／傾斜分割／…）で絞り込めて表がすっきり。
    ※.BS/.KSには分割データしか無いため、回転分割=.BS・傾斜分割=.KSで絞れます
    （再現性はCSV側にしか無く、ここでは空になります）。「グラフ指標」を選び、
    横軸（機番/日付/型式）・集計（個別/平均/最大/最小）・種類（棒/折れ線）を切替。</li>
<li><b>1件詳細</b> … いま表示中の測定を大きい偏差グラフと全指標の表でまとめて見る。</li>
</ul>
<p>どちらのタブも下の<b>CSV出力／Excel出力（表＋グラフ）／印刷</b>で出力できます。</p>
"""

FANUC_PROGRAM = """
<h3>FANUC測定プログラム作成</h3>
<p><b>プログラム作成</b>ボタンで、現在の測定条件から測定用Gコードを生成します
（設定→プレビュー→.NC保存／機械へ送信）。主な設定:</p>
<ul>
<li><b>割出軸</b>（既定 <code>Z</code>＝現在の実機。機械ごとに違うので選択式）。
    <code>X</code> はドゥエル <code>G04 X…</code> と同じ文字なので避けます。</li>
<li><b>前振り量</b> … 各測定点へ片側から接近する行き過ぎ量（バックラッシュ消し）。</li>
<li><b>振りドゥエル</b> … 振り後の待ち。測定と無関係なので小さめ（0.5〜1.0）が速い。</li>
<li><b>測定ドゥエル</b> … 測定点で静止・読取前の待ち。測定に効くので1.0〜5.0で調整。</li>
<li><b>完了信号Mコード</b> … 既定 M80。これでカウンターが値を取り込む。</li>
<li><b>カウンターリセット</b> … 先頭で0°のバックラッシュ消し＋M00（停止）。
    作業者が0にリセットしてから再開。</li>
<li><b>再現をサブプロ/1本展開</b> … 切替可。</li>
</ul>
<p><b>生成プログラムの完了信号の数＝アプリの測定シーケンス長</b>が一致することを
テストで保証しています。プレビューは「編集」で手直しでき、保存/送信はその内容を使います。</p>
<p>FANUC設定（軸・ドゥエル・クランプ等）は、<b>保存または送信したときに記憶</b>され、
次回もその設定で作れます。</p>
"""

CLAMP = """
<h3>クランプ分割測定（新機能）</h3>
<p>軸をロックして測る機械向けの機能です。プログラム作成画面で
<b>「クランプ分割」</b>をONにすると、各測定点が次の順になります（分割・再現の両方）。</p>
<p style="margin-left:1em;">
位置決め → <b>クランプ信号</b> → <b>クランプ後ドゥエル</b>（締まり待ち）<br>
→ <b>完了信号(M80)</b>（測定）<br>
→ <b>アンクランプ信号</b> → <b>アンクランプ後ドゥエル</b>（緩み待ち）→ 次の動き
</p>
<ul>
<li><b>クランプ/アンクランプ信号のMコード</b>を個別に設定できます（軸ごとに決まって
    いるので合わせる。例 4軸 M10/M11）。</li>
<li><b>クランプ後/アンクランプ後のドゥエル</b>もそれぞれ設定できます（信号を出しても
    すぐ締まらない/緩まないので待つ）。</li>
<li>完了信号は1点につき1回のままなので<b>カウンターの取込点数は変わりません</b>。
    再現のサブプロ内も同じくクランプ→読取→アンクランプになります。</li>
</ul>
<p>クランプ分割ONのときは「測定ドゥエル」は出さず、クランプ後ドゥエルが読取前の
静止待ちを兼ねます。</p>
"""

SEND = """
<h3>機械へ送信（カード不要・LAN／新機能）</h3>
<p>メモリカードで運ばなくても、生成したプログラムを<b>LAN経由で機械へ直接送れます</b>
（FANUC専用ライブラリ不要）。プログラム作成画面の下に設定と
<b>「機械へ送信」「送信の使い方」</b>ボタンがあります。方式は2つ。</p>
<ul>
<li><b>共有フォルダ（おすすめ・いちばん簡単）</b> … 機械から見えるフォルダ
    （Data Server共有 例 <code>\\\\&lt;機械IP&gt;\\nc</code> や割当ドライブ <code>Z:\\NC</code>）
    のパスを入れて送信。<code>&lt;機番&gt;.NC</code> がそのフォルダに置かれます。</li>
<li><b>FTP</b> … 機械のIP・ユーザ/パスワード・送信先フォルダを入れて送信。
    繋がらないときは「パッシブ」を切り替える。</li>
</ul>
<p>送信先・接続情報は次回も使えるよう保存されます。<b>送るだけ</b>で、安全のため
<b>運転開始（サイクルスタート）は機械側で人が行います</b>（自動起動はしません）。
うまくいかないときは「送信の使い方」内のトラブル対処を参照してください。
通信方式の比較・検討は <code>docs/FANUC通信_検討.md</code> にあります。</p>
"""

SETTINGS = """
<h3>設定</h3>
<ul>
<li><b>接続</b> … ポート（auto=自動検出）・ボーレート・パリティ。接続先X32/X31を別保持。</li>
<li><b>画面テーマ・文字サイズ</b> … OKで即反映。</li>
<li><b>ホイール/ウォーム単品のバックラッシも表示</b> … 既定OFF（総合のみ）。</li>
<li><b>保存先</b> … 測定データルートと .BS/.KS保存先（旧形式。空なら書かない）。</li>
<li><b>マスタCSV</b> … 測定条件・合否判定、ユーザー登録の回転/傾斜/再現条件。</li>
<li><b>SwitchBot 自動測定</b> … 下記参照。</li>
</ul>
<p>設定OKで保存され、開き直しても他の項目は消えません（足りないキーは既定値で補完）。
接続情報やFTPパスワードを含む settings.json はGit管理外（誤共有防止）です。</p>
"""

SWITCHBOT = """
<h3>SwitchBotで自動測定</h3>
<p>SwitchBot Bot（物理ボタン押しロボット）を<b>PCのBluetoothから直接（ハブ不要）</b>
操作し、機械の起動ボタンを押して自動測定します。</p>
<ol>
<li>SwitchBotアプリでBotを「押す(Press)」モードにし、起動ボタンの上に貼る。</li>
<li>設定の「スキャン」で近くのSwitchBotを検出して選ぶ（MAC手入力不要）。</li>
<li>必要なら「押し方」（1〜3回押し）を選ぶ。Botにパスワードがあれば入力。</li>
<li>本画面の「自動測定」で 取込→Bot押下→測定→傾き/精度判定→NGなら設定回数まで
    自動で測り直し。「空打ち」で実際に押さずリハーサルもできます。</li>
</ol>
<p>詳しい手順・トラブル対処は設定画面の「接続方法」ヘルプにあります。</p>
"""

PCORR = """
<h3>ピッチエラー補正（P補正）</h3>
<p><b>ピッチエラー補正</b>ボタンで、提出用のピッチエラー補正表＋補正後グラフを
表示し、CSV保存・印刷できます。補正間隔・単位は設定（P補正の既定値）で指定します。
通常の検査記録の印刷とは別物で、明示的に選んだときだけ付きます。</p>
<p><b>「FANUCパラメータ生成」</b>ボタンで、この補正表から<b>実機に入れるピッチエラー
補正パラメータ</b>（No.3620 基準点／3621・3622 両端点番号／3623 倍率／3624 間隔／
3625 1回転あたり移動量＋各補正点の増分データ）を作り、<b>.PRM で保存</b>できます。
測定→補正→パラメータまでこのアプリ1つで完結する（＝閉ループ）のが利点です。</p>
<ul>
<li>補正値は「絶対補正を基準点0に正規化→増分（検出単位）」に変換。回転軸は1回転で
閉じるよう増分の総和を0にします。</li>
<li>軸番号・検出単位・倍率・回転/符号・データ先頭番号は画面で調整でき、「既定にする」
で保存できます（機種差の吸収用）。</li>
<li>取り込みは人が <b>PWE=1・電源再投入</b> に注意して実施してください。値は測定から
決まった分だけを書き、捏造はしません。</li>
</ul>
<p>※番号体系・データ格納先は制御機種で細部が異なります。実機の補正設定済みサンプル
.PRM があれば、それに合わせて番号・符号・格納先を正確に調整します。</p>
"""

WEB = """
<h3>Webアプリ連携</h3>
<p>離れた場所のブラウザ（product-inspection Webアプリ）から、時間取り開始→測定→
終了の指令を受けたり、測定結果を送ったりできます（Firestoreを指令バスにする）。
Webからは型式＋モードが送られ、デスクトップ側は自分のマスタCSVから測定条件を
当てはめます。社内LAN内での利用前提です。</p>
<p><b>制御装置マスタをWebから取り込み</b>：制御装置マスタ（登録・編集）の
<b>「Webから取り込む…」</b>で、product-inspection のデータベース（Firestore）にある
制御装置情報をこのアプリの制御装置マスタへ取り込めます。データベースの中から
コレクションを選ぶと中身を読み、フィールド名（英語/日本語いろいろ）を賢く突き合わせて
号機・容量・アンプ・電圧・CNCに写します。写せなかった項目名も画面に出るので、
実データを見ながら確実に合わせられます（同じ号機は上書き、軸容量が空の号機は取り込み
ません）。※Web連携の設定（api_key等）が必要です。</p>
"""

ALARM = """
<h3>FANUCアラーム検索（新機能）</h3>
<p>ツールバーの<b>「ツール ▾」→「アラーム…」</b>で、FANUCのアラーム番号やメッセージから
意味と対処の<b>目安</b>を調べられます（ネット接続不要）。</p>
<ul>
<li><b>番号で検索</b> … <code>510</code>、<code>OT0500</code>、<code>100</code> など。
    旧番号でも拾います。</li>
<li><b>言葉で検索</b> … <code>オーバートラベル</code>、<code>電池</code>、<code>通信</code>、
    <code>overtravel</code> など、和英どちらでも。</li>
<li>左の一覧から選ぶと、右に<b>分類・原因・対処</b>が出ます。</li>
</ul>
<p>主軸アンプ番号・PMCアラーム・マクロ#3000系などは機種/機械メーカー固有です。
設定の <code>fanuc_alarm_csv</code> が指すCSV（既定 <code>マスタ/FANUCアラーム.csv</code>）に
登録すると、内蔵辞書に追加して一緒に検索できます（見出しは
コード/分類/名称/原因/対処/キーワード、和英可）。</p>
<p>⚠ ここに出るのは代表的なアラームの<b>目安</b>です。正確・最新の内容と処置は、
必ずFANUC純正の保守説明書／機械メーカーの資料に従ってください。</p>
"""

FILES = """
<h3>保存されるファイル</h3>
<ul>
<li><b>新CSV</b> … 測定データルート配下。分割と再現は別ファイル（再現は <code>機番_再現.csv</code>）。</li>
<li><b>.BS（回転分割）/ .KS（傾斜分割）</b> … 検査表システムが読む旧形式。データ行は
    実物とバイト一致、ヘッダの精度行も旧アプリと同一計算（全値一致を確認済み）。</li>
<li><b>.NC</b> … FANUC測定プログラム（保存または機械へ送信）。</li>
</ul>
"""

PROGRAM_TRANSFER = """
<h3>プログラムを機械へ渡す（カード／USBメモリ／LANケーブル）</h3>
<p>アプリで作った測定プログラム（<code>&lt;機番&gt;.NC</code>）を機械に入れて動かす手順です。
渡し方は<b>カード・USBメモリ・LANケーブル</b>の3通り。中身はどれも同じファイルです。</p>
<p><img src="overview.png" width="580"></p>

<h4>共通：アプリでの準備</h4>
<ol>
<li><b>プログラム作成</b>ボタン → 測定条件からGコードを生成（プレビューで確認・編集可）。</li>
<li><b>保存</b>で <code>&lt;機番&gt;.NC</code> を出力（保存先＝カード/USBのドライブを選べばそこへ）。
    LANは<b>「機械へ送信」</b>で直接送れます。</li>
</ol>

<h4>① メモリカードの場合</h4>
<p><img src="card.png" width="560"></p>
<ol>
<li>保存先（出力先）を<b>カードのドライブ</b>（例 <code>E:\\</code>）にして保存。</li>
<li>カードをPCから抜いて、<b>機械のカードスロット</b>に挿す。</li>
<li>機械の<b>プログラム／編集（PROG/EDIT）画面</b>で入力デバイスを「メモリカード」にし、
    ファイルを選んで<b>READ（入力）</b>。</li>
<li>読み込んだプログラムを選択して運転（サイクルスタートは人が操作）。</li>
</ol>

<h4>② USBメモリの場合</h4>
<p><img src="usb.png" width="560"></p>
<ol>
<li>出力先を<b>USBメモリのドライブ</b>（例 <code>F:\\</code>）にして保存。</li>
<li>USBを<b>機械のUSBポート</b>に挿す。</li>
<li>入力デバイスを「USBメモリ」にして、ファイルを選び<b>READ（入力）</b>。</li>
<li>プログラムを選択して運転。</li>
</ol>

<h4>機械での読込画面（カード／USB 共通のイメージ）</h4>
<p>機械のプログラム／入力画面で、入力デバイス（メモリカード／USB）を選び、ファイルを選んで
<b>READ（入力）</b>します。</p>
<p><img src="scr_prog_read.png" width="430"></p>

<h4>③ LANケーブルの場合（カード不要）</h4>
<p><img src="lan.png" width="560"></p>
<ol>
<li><b>共有フォルダ方式（おすすめ）</b>：出力先（または「機械へ送信」の送信先）を、機械から
    見える共有（例 <code>\\\\&lt;機械IP&gt;\\nc</code> や割当ドライブ <code>Z:\\NC</code>）にする。
    機械側は <b>DNC／Data Server</b> でそのフォルダから読込・運転。</li>
<li><b>FTP方式</b>：設定で機械のIP・ユーザ・パスワード・送信先フォルダを入れ「機械へ送信」。</li>
</ol>
<p>詳しくは「機械へ送信（LAN）」の項と、設定内「送信の使い方」を参照。</p>

<h4>④ DNC運転（登録せずに外部から直接動かす）</h4>
<p>DNC運転は、プログラムを制御装置の<b>メモリに登録せず</b>、カードや共有フォルダから
<b>1ブロックずつ読みながら運転</b>する方法です。<b>READ（入力）を通らないので、
「読み込めない」で止まる工程を丸ごと飛ばせます。</b>大きいプログラムを入れるときにも使います。</p>
<ol>
<li>プログラムを媒体に置く（メモリカード／USB／Data Serverの共有フォルダ）。
    アプリの「保存(.NC)」の保存先をその媒体にするか、「機械へ送信」で送る。</li>
<li><b>入出力チャネル</b>（I/O CHANNEL＝パラメータ <code>No.0020</code>）を、使う媒体に
    合わせる。目安は メモリカード <code>4</code> ／ Data Server <code>5</code>（機種により9）
    ／ RS-232C <code>0</code>・<code>1</code>。<b>正確な値は機種の説明書で確認</b>。</li>
<li>運転モードを <b>DNC（RMT／リモート）</b> にする。</li>
<li>DNC運転の画面（またはファイル一覧）で<b>動かすファイルを選ぶ</b>。</li>
<li><b>サイクルスタート</b>。運転中は媒体を抜かない。</li>
</ol>
<p style="color:#dc2626;"><b>DNCで動かすなら「再現をサブプロにする」を外してください。</b>
DNC運転中は <code>M98</code> のサブプログラム呼び出しが使えない機種があります
（呼ぶ先が制御装置のメモリに無いため）。プログラム作成画面のチェックを外すと
サブプロを使わず<b>1本に展開</b>したプログラムになり、完了信号の数（＝測定点数）は
変わりません。</p>
<p>その他、機種によってはマクロや一部のGコードに制限があります。まずは
<b>短いプログラムで1回試してから</b>本番に使ってください。</p>

<h4>アプリ側で受信して測定</h4>
<p>機械でプログラムを動かすと、ND287の読取値がアプリへ届きます。アプリは<b>「取込開始」</b>
で受信待ちにしておき、機械の完了信号（既定 <code>M80</code>）の順に値が入ります。</p>

<h4>機械が「読み込めない」ときに見るところ</h4>
<p>アプリは、<b>実機が自分で出力したプログラムと同じ形</b>でファイルを書きます。
以前の版はここが違っていて読み込めませんでした。手で編集したときは特に注意してください。</p>
<ul>
<li><b>行末の <code>;</code> を書かない</b>。画面に見える <code>;</code> は「ブロックの終わり(EOB)」の
    <u>表示</u>であって、ファイルの中では<b>改行そのもの</b>です。文字として書くと
    ISOコードに無い文字なので読込エラーになります。</li>
<li><b>コメントは英数字の大文字だけ</b>。日本語や <code>?</code>、小文字はISOコードに
    ありません。</li>
<li><b>コメントのカッコを入れ子にしない</b>。<code>(A (B) C)</code> と書くと、機械は
    最初の <code>)</code> でコメントを終わるので、残りが不正な指令になります。</li>
<li><b>O8000〜O9999 は保護領域</b>（パラメータ3202の NE8／NE9）。書込禁止だと
    転送そのものが弾かれます。アプリのサブプロは <code>O1000</code> にしてあります。</li>
<li><b>割出軸のアドレス</b>は機械に合わせる（回転テーブルは通常 <code>A</code>）。
    <code>X</code> はドゥエル <code>G04 X…</code> と同じ文字なので紛らわしく、
    X軸が無い機械では動きません。</li>
</ul>
<p>プログラム作成の画面では、上の点を<b>自動で点検</b>して、引っかかると赤い帯で知らせます。
それでも読めないときは「<b>改行(EOB)の形式</b>」を <code>CRLF</code> や <code>LF</code> に
変えて試してください（既定は実機と同じ <code>LF CR CR</code>）。</p>

<p style="color:#b45309;">※ 機械の画面表記・読込手順・デバイス名は<b>機種（0i／30i 等）で異なります</b>。
正確な操作は機械の取扱説明書で確認してください。運転は必ず安全を確認して人が行います。</p>
"""

PARAM_REWRITE = """
<h3>パラメータの書き換え 手順（制御装置の操作）</h3>
<p>製品仕様に合わせてパラメータの一部を書き換える手順です。アプリは<b>ファイルを作るだけ</b>で、
機械への入力は<b>安全のため人が機械側で行います</b>。</p>
<p><img src="param.png" width="440"></p>

<h4>① アプリで作成（受注番号から かんたん作成）</h4>
<p><b>パラメータ</b>ボタンを押すと「<b>受注番号から かんたん作成</b>」が開きます（横長画面：
左＝探す/結果、右＝号機/出力）。<b>画面上部の青い「次にやること」</b>が、今押すべき操作を
順に案内します。迷ったらそれに従えば作成まで進めます（揃うと緑で「準備OK」）。</p>
<p>初回だけ「<b>場所</b>」（製品データ・BASIC・特注パラのフォルダ）を設定します
（設定済みなら自動で畳まれます。クリックで開閉）。流れ:</p>
<ol>
<li><b>受注伝票番号(Seiban)</b> を入れて「探す」→ 製品データの場所から<b>傾斜(T)・回転(R)</b>を
    自動で探します（ファイル名の頭文字 T/R で判別）。型式しか分からない/特注を直接出したいときは
    <b>「型式で探す」</b>も使えます。</li>
<li>見つかったものに<b>チェック</b>（両方＝2軸テーブル／必要な方だけ）→ 製品データの
    <b>モーター型式の番手</b>から<b>必要容量</b>を自動判定し（αiS2/4→20A、8/12→40A、
    22/30→80A、40/50→160A の標準組合せ）、<b>作れる制御装置（号機）</b>だけを一覧に
    出します。1つ選ぶと<b>使う軸を自動で割り当て</b>ます。</li>
<li><b>出力先</b>を確認して「作成」。2軸テーブルなら<b>両軸を1ファイル</b>(<code>TR&lt;Seiban&gt;.prm</code>)
    にします。1軸なら <code>&lt;頭文字&gt;&lt;Seiban&gt;.prm</code>。</li>
</ol>
<p>検索結果は<b>「型式 / 系統 / 回転傾斜 / モーター / 容量」</b>で表示します（特注は紫＝「FANUC特注DD」、
<b>登録(DB)は緑</b>、FANUC以外は⚠で作成不可）。作成前に<b>「旧値→新値」のプレビュー</b>が必ず出るので、
確認してから書き込めます。</p>
<p><b>登録(DB)＝登録済みの変更点</b>も候補に出ます。実機からパラメータを吸い取り「詳細設定」で
<b>完成パラ − BASIC</b>を起こして登録したもの（や手入力登録）です。<b>受注番号でも型式でも</b>ヒットし、
製品データ・特注フォルダが無くても作成できます（DBに数値が入っているため）。登録のしかたは
<b>「実機から起こして登録」</b>（下記）を参照。</p>
<p><b>制御装置マスタとは</b>: 号機ごとの「CNC機種・電圧・各軸の容量」の一覧表です
（最初に渡した制御装置一覧の表をアプリ用にしたもの＝<code>マスタ/制御装置マスタ.csv</code>）。
ここに載っている号機だけが、かんたん作成の候補に出ます。</p>
<p><b>号機の登録（3通り）</b>: 画面②の「制御装置マスタ（登録/編集）…」から:</p>
<ol>
<li><b>BASICから取り込む</b>: BASICフォルダに F&lt;号機&gt;BASIC があれば、<b>軸数・容量を
    自動抽出</b>（軸名=パラメータ1020、容量=パラメータ2165。200V/400Vとも正確）。
    BASICに無い<b>電圧(200V/400V)だけ各行で選んで</b>登録します（既定200V、HV機は400V）。
    CNC機種名は任意で、後から「編集」で記入できます。</li>
<li><b>手入力で追加</b>: BASICが無い・新規購入の号機は、号機・電圧・各軸の容量を選んで登録。</li>
<li>CSVを直接編集（上級者）。いずれも変更前に <code>.bak</code> を自動保存します。</li>
</ol>
<p><b>400V(HV)</b>: モーター型式に <code>HV</code> が付いていれば400V仕様と判定し、400Vの号機
（αiSV HV／A06B-6127）だけに絞ります。400Vは電流が約半分なので容量も半分のコードで
判定します（番手8→200Vは40A・400Vは20A、番手22→80A/40A、番手40→160A/80A）。</p>
<p>必要容量は通常モーター型式から自動で決まります。標準と違うアンプを使う等の<b>例外</b>は
<code>マスタ/モーター容量マスタ.csv</code> に型式/番号/2020(モーターID)と容量を書くと上書き
できます。自動判定できないとき（番手が範囲外など）は「必要容量」を手で選べます。フォルダ設定や
手動指定、変更表CSV運用は「<b>詳細設定…</b>」（従来画面）から行えます。</p>
<p><b>リピート品</b>は「詳細設定…」→「パラメータDB…」で、登録済みから
<b>「前回と同じ制御装置で即作成」</b>（最短）か、<b>「制御装置を選んで作成」</b>を使います。</p>

<h4>② 機械へ渡す（カード／USB／LAN）</h4>
<p>プログラムと同じく、出力先を<b>カードのドライブ・USB・共有フォルダ</b>にして渡します
（上の「プログラムを機械へ渡す」の①②③と同じ媒体）。</p>

<h4>③ 書込許可（PWE=1）</h4>
<ol>
<li>機械の<b>設定（SETTING）画面</b>を開く（OFS/SET → SETTING など）。</li>
<li><b>「パラメータ書込」＝1</b> にする。<code>SW0100</code>（パラメータ書込許可）の警告が出ますが、
    これは<b>正常</b>です。</li>
</ol>
<p><img src="scr_setting.png" width="430"></p>

<h4>④ 入力</h4>
<ul>
<li><b>ファイルで入力</b>：パラメータ画面でカード/USB/共有から <code>.prm</code> を READ（入力）。
    載っている番号だけが書き換わり、他は現状維持です。</li>
<li><b>手入力</b>：<b>確認表</b>を見ながら、該当番号だけを手で入力（旧値→新値を照合）。</li>
</ul>
<p><img src="scr_param.png" width="430"></p>

<h4>⑤ 電源再投入（必要なとき）</h4>
<p>一部の番号は<b>電源を入れ直す</b>まで反映されません（<code>PW0000</code> 等の警告が出たら再投入）。</p>

<h4>⑥ 確認して書込禁止に戻す</h4>
<p>値が正しく入ったか確認表で照合し、<b>「パラメータ書込」＝0</b> に戻します。</p>

<h4>セミクロ／フルクロの注意</h4>
<p>同じ型式・同じ番号でも<b>セミクロ／フルクロで値が違う</b>ことがあります。アプリのDBは
パラメータ <b>1815</b>（別置検出器）で自動判別して表示します。製品はフルクロでも、メカ精度を
セミクロで測ることがあるので、取り違えに注意してください。</p>

<h4>パラメータを見る／比較</h4>
<p>「パラメータを見る/比較…」で、<b>BASIC・製品データ・作成した.prm の中身を番地ごとに一覧</b>
できます（スクロール）。<b>2ファイルを選ぶと差分を色付きで強調</b>し、上の「差分のみ」で
違う番地だけ、番号欄に <b>1000〜3000</b> 等を入れると<b>その範囲だけ</b>表示します。
FANUCネイティブ(.PRM)もヘッダ+CSV(製品)も読めます。</p>

<h4>対応する制御（FANUCのみ）</h4>
<p>パラメータ作成は<b>FANUC制御のみ対応</b>です（DDモーター=DiS系もFANUC形式なので対象）。
製品データには<b>三菱(MELSERVO/MDS)・安川(TPC)</b>など別メーカーも混在します。これらは
かんたん作成で<b>系統を自動判定して『FANUC以外』と表示・既定オフ</b>にし、誤って作成しても
<b>ブロック</b>します（FANUCのBASICに別系統の値を書いてしまわないため）。三菱・安川は後日対応。
DDモーター(DiS)の容量は番手＝トルクで自動判定できないため、容量は手入力かモーター容量
マスタで指定してください（作成自体はFANUC形式なので可能）。</p>

<h4>特注パラ（Excel）・DDモーター</h4>
<p>MKPRM.xlsで作れない<b>特注パラ</b>（DDモーター等）は、機種別フォルダに<b>Excelシート</b>で
保存されています。「特注パラの場所」を指定し、<b>「索引を更新」を一度押して登録</b>しておくと、
以後は索引から高速に引けます（毎回フォルダを解析しません）。</p>
<p><b>登録すると、パラメータの数値そのものも索引に取り込みます</b>。だから登録後は
<b>特注パラフォルダが無くても（共有サーバがオフラインでも）作成できます</b>。
元Excelの<b>場所も索引に残す</b>ので、何かあったときはすぐ元ファイルを開いて調べられます。
<b>新しい特注パラが入った／中身を直したときは、もう一度「索引を更新」を押してください</b>
（そのとき最新の数値が取り込まれます）。</p>
<p>特注パラには<b>受注伝票番号が無い</b>ため、<b>型式</b>で照合します:</p>
<ul>
<li><b>受注番号で探す</b>と、製品.prmから分かった<b>型式で特注パラも自動照合</b>して一緒に候補に出ます
    （紫色＝特注、DDは「FANUC特注DD」）。</li>
<li><b>「型式で探す」</b>欄に型式（例 RTT-135 / MZF-50010）を入れると、特注パラを直接候補に出せます
    （受注番号が無い・特注だけ見たいとき）。作成時の出力名用に Seiban も入れてください。</li>
</ul>
<p>選んで作成すると、シートの<b>番地→設定値</b>をBASICの割当軸へ書き込みます（製品.prmと同じ流れ）。
※製品データ(.prm)はファイル名一致だけで探すので索引は不要・高速です。</p>
<ul>
<li><b>DD判別</b>: 番地2300のDD制御ビットで自動判定（ギア1/1・モータ"Dis"も指標）。</li>
<li><b>容量</b>はDDだと型式から自動判定できないので「手で選択」になります（必要容量を選ぶ）。</li>
<li>そのDD/特注に<b>対応したBASIC（その機種の制御装置）</b>を使ってください。BASICに無い番地は
    「未反映」で一覧に出ます。Excelが無く<b>PDFのみ</b>の機種は対象外（従来どおり印刷・手入力）。</li>
</ul>

<h4>実機から起こして登録（パラメータDB）</h4>
<p>すでに動いている制御装置から、その製品のパラメータを<b>逆に起こして登録</b>できます。
仕組みは「<b>実機の完成パラ − その制御装置のBASIC ＝ その製品の変更点</b>」です（足し算の逆）。
一度登録すれば、次回は<b>製品データが無くても</b>受注番号・型式から作れます（かんたん作成の
候補に<b>緑の「登録(DB)」</b>で出ます）。</p>
<ol>
<li><b>吸い取り</b>：実機からFANUCのツール／RS-232でパラメータを<b>ファイル出力</b>します
    （アプリは安全のため<b>CNCを直接読みません</b>。実機操作は人が実施）。出てきた
    <b>完成済み .prm（ネイティブ形式 <code>N#####Q1…</code>）</b>を用意します。
    ※貼り付けは改行・空白が落ちるので、必ず<b>ファイル</b>で。</li>
<li>「ツール ▾」→「パラメータ…」→<b>「詳細設定」</b>を開く。</li>
<li><b>「使うBASIC」＝その制御装置のBASIC</b>、<b>「製品データ」＝吸い取った完成 .prm</b> を指定。
    型式も入れておくと登録名になります。</li>
<li><b>「FANUC .prm 作成」</b>を押すと、差分（変更点）で作成し、同時に<b>パラメータDBへ自動登録</b>
    します（型式・制御装置・モーター・ギア比つき）。</li>
</ol>
<p>注意：自動登録されるのは<b>ネイティブ形式の完成 .prm</b>（本当の変更点）のときだけです。
差分は<b>確定したビット値</b>で登録されます（製品ファイルの「不問ビット <code>*</code>」のような汎用化は
しません）。<b>同じ制御装置（同じBASIC）に戻すなら完全一致</b>。別系統のBASICへ当てるときは、
本来は継承すべきビットを上書きしうる点に注意してください。手入力で1件ずつ登録することもできます
（パラメータDB →「新規手入力」）。</p>

<h4>2軸テーブル（傾斜＋回転）</h4>
<p>傾斜軸と回転軸を持つ<b>2軸テーブル</b>は、<b>1つの制御装置(BASIC)に両軸ぶんを入れて
1ファイル</b>にします。傾斜だけ／回転だけの<b>フルbackupを2ファイル</b>に分けて作ると、
あとから読み込んだ方が<b>もう片方の軸をBASIC値へ戻して</b>しまうため、必ず1ファイルに
両軸を入れます（ファイル名は <code>TR&lt;Seiban&gt;.prm</code>）。</p>
<ul>
<li><b>製品データ（完成済み.prm）から</b>：その .prm が傾斜軸・回転軸の両方を変更していれば
    アプリが<b>自動で検知</b>し、両軸を1ファイルにします（作成前プレビューで両軸を確認）。</li>
<li><b>登録済みエントリから</b>：「パラメータDB…」で1軸目を選び、<b>「2軸で作成（傾斜＋回転）」</b>
    で相手エントリ（もう1軸）と各軸を指定すると、1ファイルにまとめて作成します。</li>
</ul>
<p>各軸は<b>容量・空き軸で決まった軸番号</b>へ入ります。作成後は軸ごとに作成ログ・DB履歴が
残ります。<b>軸の取り違えに注意</b>（傾斜と回転を逆に入れない）。</p>

<h4>機械が「読み込めない」ときに見るところ（パラメータ）</h4>
<p>測定プログラムと同じで、<b>実機が自分で出力したファイルと同じ形</b>で書く必要があります。
アプリは作成時に自動で点検し、引っかかると確認を出します。</p>
<ul>
<li><b>ブロックの区切り</b>は実機と同じ <code>LF CR CR</code>。元にした BASIC が PC の
    改行（LFのみ／CRLF）でも、出力時にそろえます。</li>
<li><b>拡張子</b>は既定で <code>.DAT</code>（実機が出力するのがこの形）。
    <code>.prm</code> が読めなかった実績があるため。設定 <code>param_out_ext</code> で変更可。</li>
<li><b>その制御装置に無い番号を書かない</b>。設定の「マスタ/バックアップ」に<b>実機の
    バックアップ</b>を入れておくと、そこに無い番号を作成前に指摘します。
    「何番から先がメーカ用」という固定の線引きではなく、<b>実機どうしの突き合わせ</b>で
    判定します（実例: BASIC 側に実機に無い N09802・N09820〜N09999 の181個があった）。</li>
<li><b>ASCII以外の文字・<code>;</code> を書かない</b>。</li>
</ul>

<h4>出荷ファイルで0にする項目</h4>
<p>次は<b>機械個体の実測値</b>なので、前の機械の値を持ち込まないよう
<b>作成時に全軸を0</b>にします（作成前プレビューに旧→新が出ます）。</p>
<ul>
<li><code>1850</code> グリッドシフト（原点の実測補正）</li>
<li><code>1851</code> バックラッシ補正量 ／ <code>1852</code> 早送り時のバックラッシ補正量</li>
</ul>
<p>作る軸だけでなく<b>全軸</b>を0にします。作る軸だけだと、他の軸に前の機械の値が
残ったまま出荷されてしまうためです。かんたん作成の「グリッドシフト・バックラッシ補正を
0にする」で切り替えられ、番号は設定 <code>zero_individual_params</code> で変更できます。</p>
<p><b>原点確立（<code>1815 #5</code> APZ）</b>は別枠で、「原点確立」の選択で
ON/OFF します（0にする対象には含めていません）。</p>

<h4>ソフトリミット（1320 / 1321）を入れるか選ぶ</h4>
<p>客先パラメータには<b>傾斜軸の可動範囲</b>が
<code>1320</code>（＋側）・<code>1321</code>（−側）に入っています。ところが
<b>検査のときは入れたくない</b>ことがあります（検査の割出しが範囲外になって動かせない）。
作成画面の「ソフトリミット」で3つから選べます。</p>
<table border="1" cellspacing="0" cellpadding="4">
<tr><th>選択</th><th>何をするか</th><th>使うとき</th></tr>
<tr><td><b>客先どおり入れる</b>（既定）</td><td>客先の値をそのまま書く</td>
<td>出荷するファイル</td></tr>
<tr><td><b>入れない</b></td><td>1320/1321 だけ書かず、BASICの値をそのまま残す。
他の番号は普通に入る</td><td>BASICから作る検査用</td></tr>
<tr><td><b>無効化する</b></td><td><b>作る軸だけ</b>＋側 <code>-1</code>／−側 <code>+1</code>
に書き換える</td><td>客先値が既に入ったファイルを元にするとき</td></tr>
</table>
<p><b>-1 / +1 の根拠</b>: 手元にある BASIC <b>31本すべて</b>が
<code>1320=-1</code>・<code>1321=+1</code>（使っていない軸は <code>0.0</code>）で、
それ以外の値は1つもありませんでした。つまり<b>BASICの出荷時の状態がこの値</b>です。
「入れない」と「無効化」は、BASICから作るかぎり同じ結果になります。</p>
<p>「無効化」は<b>作る軸だけ</b>です。全軸に書くと、同じファイルに入っている
相手軸（2軸テーブルの回転軸など）の設定まで消してしまうためです。
小数点の有無（<code>-1</code> か <code>-1.0</code>）は<b>ファイルの書き方に合わせます</b>
（旧書式の8本は <code>A1 P-1</code>、新書式は <code>A1P-1.0</code>）。
変わる所は必ず<b>作成前プレビュー</b>に出ます。初期値は設定
<code>soft_limit_mode</code>、番号は <code>soft_limit_params</code> で変えられます。</p>

<h4>出力ファイル名</h4>
<p>作ったファイルの名前に<b>型式・軸・傾斜/回転</b>が入ります。
フォルダにまとめて作っても、開かずに中身が分かります。</p>
<pre>T50013078_RTT-135_Z-TILT.DAT      傾斜 / Z軸
TR50013078_MZF-50010_Z-TILT_A-ROT.DAT   2軸テーブル（1ファイルに両軸）</pre>
<p>制御装置で見る名前なので<b>半角英数字だけ</b>にします（全角で入力した型式は
半角へ直し、漢字・カナは落とします）。<code>傾斜=TILT</code>・<code>回転=ROT</code>。
名前が長くて制御装置で扱いにくいときは、設定
<code>param_name_detail</code> を <code>false</code> にすると
従来の <code>T50013078.DAT</code> に戻ります。</p>
<p>※完成製品の差分から2軸を作る場合だけは、どちらの軸が傾斜かがファイルに
書かれていないため、軸名だけを入れます（頭文字 <code>TR</code> が傾斜＋回転を表します）。</p>

<p style="color:#dc2626;"><b>★安全のために必ず守ること</b></p>
<ul>
<li style="color:#dc2626;"><b>軸を間違えない</b>（容量で決まった、その制御装置で使う軸だけ書く）。</li>
<li style="color:#dc2626;">書き換え前に<b>バックアップ</b>を取る。</li>
<li style="color:#dc2626;">確認表で<b>旧値→新値を照合</b>してから入力。<b>未検証のまま投入しない</b>。</li>
<li style="color:#dc2626;">画面表記・キー操作・読込手順は<b>機種で異なります</b>。必ず機械の取扱説明書で確認。</li>
</ul>
"""

FTP_AND_DNC = """
<h3>FTP転送とDNC運転（かみくだいた説明）</h3>

<h4>そもそもFTPとは</h4>
<p><b>ファイルを渡すための、昔からある決まりごと</b>です。難しいことはしていません。
「<b>制御装置の中にフォルダがあって、そこへPCからファイルを置きにいく</b>」だけ、と
思ってください。カードで運ぶ代わりにLANケーブルで運ぶ、それだけです。</p>
<p>制御装置に<b>組込みイーサネット</b>（本体に最初から付いているLANポート）があれば、
たいてい<b>FTPサーバ</b>の機能が入っています。サーバといっても
「PCからファイルを置ける口が開いている」という意味です。オプションの
Data Server や Fast Ethernet と違い、<b>追加の基板なしで使えます</b>。</p>

<h4>使うために要るもの（4つだけ）</h4>
<ol>
<li><b>機械のIPアドレス</b>（例 192.168.0.10）… 機械側の「設定（埋込みEther）」画面で決める</li>
<li><b>ユーザ名・パスワード</b> … 機械側で決める。空でよい機械もある</li>
<li><b>置き先フォルダ</b> … 空ならいちばん上（ルート）</li>
<li><b>PCと機械がLANでつながっていること</b> … 同じネットワークにいること</li>
</ol>
<p>この4つをアプリの「プログラム作成」→「機械へ送信」の設定に入れて
<b>「機械へ送信」</b>を押すだけです。<b>カードは要りません。</b></p>

<h4>やってみる手順</h4>
<ol>
<li>機械側で 設定（埋込みEther）を開き、<b>IPアドレス・サブネットマスク</b>を確認/設定。</li>
<li>PCから <code>ping 192.168.0.10</code> が通るか確認（通らなければ配線かIPの問題）。</li>
<li>アプリのプログラム作成画面 → 方式で <b>FTP</b> を選び、IP・ユーザ・パスワードを入力。</li>
<li><b>「機械へ送信」</b>。成功すると送信先が表示されます。</li>
<li>機械側でファイル一覧を見て、届いているか確認。</li>
</ol>
<p>つながらないときは<b>「パッシブ」</b>のチェックを切り替えてみてください。
機械によってどちらでないと通らないかが違います。</p>

<h4>「FTPで送る」と「DNC運転」は別のこと</h4>
<p>ここが混ざりやすいところです。</p>
<ul>
<li><b>FTPで送る</b> … ファイルを機械へ<b>置く</b>だけ。まだ動きません。</li>
<li><b>DNC運転</b> … 置いたファイルを<b>メモリに登録せずそのまま動かす</b>。</li>
</ul>
<p>ふつうは「送る → 機械のメモリに<b>読み込む(READ)</b> → 運転」ですが、
<b>読み込みで止まる</b>ことがあります。
<b>DNC運転はその読み込みを飛ばして、置いたファイルを直接動かす</b>やり方です。</p>

<h4>FTPファイル転送でのDNC運転とは</h4>
<p>言葉が長いだけで、やることは
「<b>機械が、LANの向こうにあるファイルを読みながら運転する</b>」です。
向きが2通りあります。</p>
<ul>
<li><b>PC→機械へ置いてから動かす</b> … FTPで機械の中へ置き、そこからDNC運転する。
    いちばん分かりやすい。</li>
<li><b>機械がPCを見にいく</b> … 機械側に「PCのIP・共有フォルダ・ユーザ」を登録しておき、
    運転時に機械がPCからブロックを読みながら動く。PCに置いたままでよい。</li>
</ul>
<p><b>おすすめは前者</b>です。機械にPCの情報を登録する必要がなく、
アプリの「機械へ送信」がそのまま使えます。</p>

<h4>運転モードについて</h4>
<p>DNC運転は <b>MDI ではありません</b>。MDIは手打ちで1行入れるモードです。</p>
<p>RMT（リモート）の位置がある機械はRMTへ。<b>RMTが無い機械は
MEM（AUTO／メモリ）のまま</b>、プログラム画面でDNC運転するファイルを選び、
その状態でサイクルスタートします。</p>

<h4 style="color:#b91c1c;">★うちの試験装置ではDNC運転ができない可能性が高い</h4>
<p>試験装置の<b>操作モード</b>スイッチを実際に確認しました。位置は8つで、
<b>DNC（RMT／リモート）がありません</b>。</p>
<pre>手動早送り ／ 手動送り ／ 原点復帰 ／ MDI
ハンドル ／ メモリ運転 ／ 記憶編集 ／ 自動測定</pre>
<p>FANUCのDNC運転は、<b>メモリ運転(MEM)モード</b>に入れたうえで
<b>DNCI という信号</b>（PMCアドレス <code>G0043</code> の <code>#5</code>）が
ONになって初めて始まります。ふつうは操作盤の「DNC」ボタンがこの信号を入れます。
<b>このスイッチにその位置が無い＝装置の配線（ラダー）がDNCを想定していない</b>
可能性が高く、その場合はいくら設定してもDNC運転は始まりません。</p>

<h4>2分でできる切り分け（DNCI信号を見る）</h4>
<ol>
<li>PMCの信号状態の画面を開く
  <ul>
  <li>0i-MD / 31i / 35i … <code>SYSTEM</code> →［PMCMNT］→［信号状態］</li>
  <li>18i-M / 18i-B / 18i-MB … <code>SYSTEM</code> →［PMC］→［PMCDGN］→［STATUS］</li>
  </ul></li>
<li>アドレスに <code>G0043</code> を入れる</li>
<li><b>操作モードをメモリ運転にして</b>、DNCらしきボタンを押しながら
    <code>#5</code> のビットを見る</li>
</ol>
<p><b>#5 がずっと 0 のまま</b>なら、この装置ではDNC運転はできません（配線の話なので
パラメータでは直りません）。<b>1 になる</b>なら、あとは設定だけの問題です。</p>
<p>参考: 手元のBASIC 31本を調べたところ、<b>全台で
<code>N00020</code>（入出力チャネル）= 4 ＝メモリカード</b>でした。
つまり「カードから読む」側の設定は最初からできています。
また <code>N00138</code> は全台 <code>00000000</code> でした
（この番号の <code>#7</code> がメモリカードDNCの有効ビットだと理解していますが、
これは<b>裏が取れていません</b>。触る前に機械の説明書で番号を確認してください）。</p>

<h4>この装置での現実的なやり方</h4>
<p>DNCが使えないなら、<b>記憶編集でメモリに登録 → メモリ運転で実行</b>が正解です。
測定プログラムは5KB程度なので、登録はすぐ終わります。カードから読めない問題は
別に解決済み（区切り・拡張子・カードの中身）なので、READで困ることはもう無いはずです。</p>

<h4>DNC運転のときの注意（重要）</h4>
<p style="color:#dc2626;"><b>「再現をサブプロにする」のチェックを外してください。</b>
DNC運転中は <code>M98</code> のサブプログラム呼び出しが使えない機種があります
（呼ぶ先が機械のメモリに無いため）。外すと1本に展開され、
完了信号の数（＝測定点数）は変わりません。</p>
<ul>
<li>運転中は媒体・LANを切らない。</li>
<li>組込みイーサネットは速度が控えめ。測定プログラムは5KB程度なので問題ないが、
    非常に大きいプログラムだと送りが追いつかないことがある。</li>
<li>マクロや一部のGコードに制限がある機種がある。<b>まず短いプログラムで1回試す。</b></li>
</ul>

<h4>どれを使えばいいか</h4>
<ul>
<li>とりあえず動かしたい … カード＋メモリへREAD（従来どおり）</li>
<li><b>READで読み込めない</b> … <b>DNC運転</b>（読み込みを飛ばせる）</li>
<li>カードを運ぶのが面倒 … <b>FTPで送信</b>（組込みイーサネットがあれば）</li>
<li>カードのフォルダが見えない … 一括作成で<b>「1つのフォルダに平置き」</b></li>
</ul>
<p style="color:#b45309;">※ メモリカードのサブフォルダを開けない制御装置があります
（古い機種はルートだけのことが多い）。画面表記・パラメータ番号・対応可否は
<b>機種で異なります</b>。必ず機械の取扱説明書で確認してください。</p>
"""


TROUBLESHOOT_HISTORY = """
<h3>実機で分かったこと（記録）</h3>
<p>実機で「読み込めない」が続いたときに、渡されたファイルを1バイトずつ突き合わせて
原因を特定した記録です。<b>同じことが起きたらここを見れば分かる</b>ようにしてあります。
アプリ側はすべて対策済みですが、<u>なぜそうしているか</u>を残します。</p>

<h4>1. 測定プログラムが制御装置で読めなかった</h4>
<p><b>症状</b>: アプリで作った .NC を機械に入れても読み込めない。
実機が出したプログラムは読める。</p>
<p><b>調べ方</b>: 両方をバイトで比較した。違いは4つ。</p>
<ul>
<li>行末の <code>;</code> … 実機 <b>0個</b> / アプリ <b>302個</b></li>
<li>ブロックの区切り … 実機 <code>LF CR CR</code> / アプリ <code>LF</code> のみ</li>
<li>コメント … 実機 大文字英数字のみ / アプリ <code>(MEASURE ????)</code></li>
<li>コメントのカッコ … 実機 入れ子なし / アプリ <code>(--- A (B) ---)</code></li>
</ul>
<p><b>原因</b></p>
<ul>
<li>画面に見える <code>;</code> は「ブロックの終わり(EOB)」の<u>表示</u>であって、
    ファイルの中では<b>改行そのもの</b>。<code>;</code> はISOコード表に無い文字なので、
    書き込むと読込エラーになる。</li>
<li>日本語のモード名をASCII化していたため <code>????</code> になっていた。
    <code>?</code> もISOコードに無い。</li>
<li>FANUCは<b>最初の <code>)</code> でコメントを終える</b>ので、
    <code>(--- RESET (set 0) ---)</code> と書くと残りが不正な指令として読まれる。</li>
<li>プレビュー欄を経由した時点で CR が落ちていた。</li>
</ul>
<p><b>対策</b>: 生成・保存・送信すべてを同じ整形に通し、実機と同じ形で書く。
作成画面で<b>自動点検</b>し、引っかかると赤帯で知らせる。
読めなかったファイルは6件検出、実機のファイルは指摘なし。</p>

<h4>2. 割出軸のアドレスが違っていた</h4>
<p>既定が <code>X</code> だったが、<code>X</code> はドゥエル <code>G04 X…</code> と
同じ文字で紛らわしく、実機の割出軸は <code>Z</code> だった（機械ごとに違う）。
選択式にして既定を <code>Z</code> に変更。<code>X</code> を選ぶと警告が出る。</p>
<p>再現サブプロのO番号も <code>O9001</code> → <code>O1000</code> へ。
<b>O8000〜O9999 は保護領域</b>（パラメータ3202 NE8/NE9）で、書込禁止だと
<u>転送そのものが弾かれる</u>ため。</p>

<h4>3. パラメータファイルが読めなかった（.prm はダメ / .DAT は通る）</h4>
<p><b>原因は2つ</b></p>
<ul>
<li><b>区切りが違う</b>。読める方は <code>LF CR CR</code>、読めない方は
    <code>LF</code> のみ。アプリは<u>元にしたBASICの改行をそのまま引き継いで</u>
    いたため、PC側の改行のBASICから作った製品ファイルは全部この形になっていた。</li>
<li><b>その制御装置が持っていない番号が181個入っていた</b>
    （N09802・N09820〜N09999）。実機が出したバックアップには無い番号で、
    書き戻すと取込が止まる。</li>
</ul>
<p style="color:#b45309;">※「9000番台はメーカ用だから」という固定の線引きは
<b>誤り</b>でした。この制御装置は N27603 まで番号があり、9000番台も大半は実在します。
<b>実機のバックアップと突き合わせて</b>判定するのが正しいやり方です。</p>
<p><b>対策</b>: N形式の出力は必ず実機の区切りで書く。出力の既定拡張子を
<code>.DAT</code>（実機が出す形）に。作成前に点検し、実機に無い番号は
<b>その場で除いて作れる</b>ようにした。実機のバックアップが手元に無くても、
フォルダにある実機BASIC全部の「番号の和集合」と比べて判定できる。</p>

<h4>4. 拡張子では出どころが分からない</h4>
<p>実機BASIC 31本を調べた結果、<b>拡張子は当てになりません</b>。</p>
<ul>
<li><code>F82BASIC.PRM</code> … 実機が出したもの</li>
<li><code>F18〜F21BASIC.DAT</code> … PCで作られたもの</li>
<li><code>F22BASIC.txt</code> … 実機が出したもの</li>
<li><code>F35BASIC</code>（拡張子なし） … 実機が出したもの</li>
</ul>
<p><b>見分け方は中身の区切り</b>です。<code>LF CR CR</code> なら実機、
<code>LF</code>／<code>CRLF</code> ならPC製。
<code>CRLF×3</code> は実機のものをWindowsのテキストモードで写したもの。
パラメータ画面の<b>「出どころ判定...」</b>でフォルダごと調べられます。</p>
<p style="color:#dc2626;"><b>選択の優先順位のバグ</b>: 号機からBASICを選ぶとき
拡張子（.prm 最優先）で決めていたため、F23 のように実機の .DAT とPC製の .prm が
並んでいると<u>読めなかった .prm を掴んでいました</u>。中身で決めるよう修正済み。</p>

<h4>5. BASICの書式が2世代ある</h4>
<pre>新（F18以降）: N01825Q1A1P3000A2P3000
旧（F10〜F17）: N01825 A1 P 3000 A2 P 3000</pre>
<p>アプリは <code>Q1</code> がある前提だったため、<b>8台ぶんのBASICを
N形式と認識できず</b>、製品値が入らないままマスタとも違うファイルを出していました。
両方に対応済み（実機BASIC 31本すべてで、同じ値を書けばバイト不変・
1点差替えは1行だけ変化することを確認）。</p>

<h4>6. 中身が同じBASICは「取り違え」ではない</h4>
<p>31本のうち6組がバイト完全一致でした。当初これを取り違えとして警告しましたが、
<b>BASICは基本パラメータなので、仕様が同じ号機は中身も同じになるのが正常</b>です。</p>
<p>号機マスタの<b>ハード仕様</b>（CNCユニット・制御電圧・軸ごとの容量）と
突き合わせ、<b>仕様が違うのに中身が同じ</b>組だけを知らせるようにしました。
Ver や SERVO の版数はソフトの版なので見ません。</p>

<h4>7. 号機で違うパラメータはどこにあるか</h4>
<p>実機BASIC 10台を総当たりで突き合わせた結果（積算カウンタと原点は除外）、
差は281箇所。内訳は次のとおり。</p>
<ul>
<li><b>仕様で説明できる 204箇所(73%)</b> … アンプ58／SERVO版数59／
    容量+アンプ+SERVO 31／CNCユニット16 ほか</li>
<li><b>サーボ調整値 36箇所</b> … 2000番台。仕様では決まらない（実測・調整の結果）</li>
<li><b>オプション 31箇所</b> … N14552〜14637。F31とF82だけON。号機マスタに項目が無い</li>
<li>その他 10箇所</li>
</ul>
<p>仕様が完全一致するペア（F85-F86）でも<b>39箇所違い</b>、うち36箇所がA2軸の
サーボ調整値でした。<b>「同じ容量なら差が無い」は成り立ちません。</b></p>
<p>逆に<b>容量と1対1に対応していたのは <code>N2165</code>（アンプ最大電流）</b>
です（<code>AMR = 容量 + 5</code>。20A→25／40A→45／80A→85／160A→165）。
これで BASIC と号機マスタを照合できます。<b>2165 はモーター形式(2020)からは
決まりません</b>（同じ255で25と45の両方が実在）。アンプ側の情報です。</p>

<h4>8. 個体データは持ち込まない</h4>
<p>仕様が同じでも<b>原点は据付けごとに違います</b>。次はその機械の実測値なので、
出荷するファイルには残しません。</p>
<ul>
<li><code>1850</code> グリッドシフト（原点の実測補正）</li>
<li><code>1851</code>／<code>1852</code> バックラッシ補正量</li>
</ul>
<p><b>作成時に全軸を0にします</b>（作る軸だけだと他の軸に前の機械の値が残るため）。
0にする箇所は作成前プレビューに旧→新で出ます。
<code>1815 #5</code>（APZ＝原点確立済み）は「原点確立」の選択で別途扱います。</p>

<h4>9. 同じ番号が2回出るファイルがある</h4>
<p>F35BASIC は 2000番台を中心に<b>3731個の番号が2回</b>出ていました（多系統）。
アプリは<b>最初の1つだけ</b>を読み書きするので、2つ目には製品値が入りません。
点検で知らせます。</p>

<h4>10. 途中に <code>%</code> があるファイル（読取がそこで終わる）</h4>
<p><code>%</code> は<b>読取終わりの記号</b>です。ファイルの途中にあると、
制御装置はそこで読むのをやめ、<b>その先が丸ごと読み込まれません</b>。</p>
<p>実データでは <code>F35BASIC</code> の10958行目が <code>%1P00000000</code> に
化けており（本来 <code>N?????Q1P00000000</code>）、<b>その先の258行
（N27124〜N27609）が読まれない</b>状態でした。点検で知らせます。
<b>F35は実機から取り直してください。</b></p>

<h4>11. 号機マスタとBASICが食い違っていた</h4>
<p>容量照合で7本に食い違いが出ました（F14 F16 F17 F21 F25 F26 F31）。
実機のファイルが正なので<b>マスタが古い</b>と判断し、
出どころ判定の画面に<b>「マスタをBASICに合わせる...」</b>を用意しました。
根拠にするのは<b>実機が出したBASICだけ</b>で、PC製は使いません。</p>

<h4>12. 値の書き方が実機と違っていた（※読めなかった原因ではありません）</h4>
<p style="color:#b91c1c;"><b>訂正</b>: 最初これを「読めなかった原因」と書きましたが
<b>間違いでした</b>。本当の原因は次の 13.（カードの中身）です。
このファイルは中身を直さなくても、カードを空けたら制御装置に出てきました。
ここに書くのは「実機の書き方と違ったのでそろえた」という話です。</p>
<p>区切りも <code>%</code> も文字も番号もすべて合格で、
違ったのは<b>値の書き方</b>だけでした。</p>
<pre>N01320Q1A1P+108.000   ← ＋符号つき・小数3桁
N01260Q1A1P360.000    ← 小数3桁</pre>
<table border="1" cellspacing="0" cellpadding="4">
<tr><th></th><th>読めなかった出力</th><th>実機BASIC 31本</th></tr>
<tr><td>＋符号つきの値</td><td>1個</td><td><b>0個</b></td></tr>
<tr><td>小数2桁以上</td><td>10個</td><td><b>0個</b>（80,185個すべて1桁）</td></tr>
</table>
<p>製品データ（人が読む用の <code>.prm</code>）は <code>+108.000</code> のように
<b>表示用の書き方</b>をしていて、それが実機用のファイルへそのまま書かれていました。
<code>;</code> のときと同じ「表示用の形が機械用ファイルに漏れた」パターンです。</p>
<p><b>対策</b>: 書き込む前に実機の書き方へそろえます（<b>値は変えません</b>）。
<code>+108.000</code>→<code>108.0</code>、<code>108.125</code> はそのまま。
書く前の点検でも「＋が付いている」「小数が2桁以上」を知らせます。</p>
<p><b>旧書式（Q1なしの8台）では値に触りません。</b>この世代は小数を1つも使わず
（5万個すべて整数＝最小設定単位）、小数を機械的に落とすと<b>1000倍ずれる</b>ためです。
代わりに点検で「単位が食い違っていないか確認」と出します。</p>

<h4>13. ★制御装置に「見えない」の本当の原因はカードの中身だった</h4>
<p>作ったパラメータが制御装置の画面に出てこなかった件の<b>実際の答え</b>です。</p>
<p style="background:#fef2f2; padding:6px; border:1px solid #fecaca;">
カードに入っていたファイルを<b>全部消して、そのファイル1本だけ</b>にしたら、
そこで初めて制御装置の画面に出てきました。<b>長い名前でも出ました</b>。</p>
<ul>
<li>ファイルの<b>中身は関係なかった</b>（12. の＋符号や小数は原因ではありません）</li>
<li>ファイル<b>名の長さも関係なかった</b>（<code>TR50014175</code> の10文字でも出た）</li>
<li>カードに入っているファイルの<b>量</b>が効く</li>
</ul>
<p><b>「PCで書けた」＝「制御装置に見える」ではありません。</b>
エクスプローラで見えていても、制御装置の画面には出てこないことがあります。</p>
<p>空き容量が効いたのか、ファイルの<b>数</b>（FATのディレクトリ枠。長い名前は
1ファイルで複数の枠を使います）が効いたのかは切り分けていません。
どちらもありうるので、アプリは<b>両方</b>を作成前に出します。</p>
<ul>
<li>作成前プレビューと保存時に <b>「ファイル○個 / 空き○MB」</b> を表示</li>
<li>空きが足りない・ファイルが100個以上なら<b>赤字で警告</b></li>
<li>測定プログラムの保存でも同じ確認（同じカードへ書くため）</li>
</ul>
<p><b>困ったときの手順</b>: 制御装置にファイルが出てこなかったら、中身を疑う前に
<b>カードを空にして1本だけで試す</b>。1分で切り分けられます。
普段から、カードには<b>使うファイルだけ</b>を入れておくのが安全です。</p>

<h4>調べ方（同じことが起きたら）</h4>
<ol>
<li><b>まずカードを空にして1本だけで試す</b>（置き場所の問題を先に外す）。</li>
<li><b>実機が出したファイルを1つ手に入れる</b>。これが唯一の正解の見本。</li>
<li><b>バイトで比べる</b>。テキストエディタでは違いが見えない（改行・不可視文字）。</li>
<li>区切り → 使えない文字 → 番号の集合、の順に見る。</li>
<li>アプリの<b>「出どころ判定...」</b>と作成時の<b>自動点検</b>が、
    ここで挙げた項目を機械的に調べます。</li>
</ol>
"""


HELP_SECTIONS = [
    ("このアプリについて", APP_OVERVIEW),
    ("測定モード", MODES),
    ("測定の進め方", MEASURE_FLOW),
    ("画面に出る結果", RESULTS),
    ("JIS B 6190-2 評価【新】", ISO230),
    ("バックラッシ手動補正", BACKLASH_CORRECTION),
    ("温度別の合否判定", TEMP_JUDGE),
    ("過去データ（.BS/.KS）", PAST_DATA),
    ("分析（横断比較・1件詳細）", ANALYSIS),
    ("FANUC測定プログラム作成", FANUC_PROGRAM),
    ("クランプ分割測定【新】", CLAMP),
    ("機械へ送信（LAN）【新】", SEND),
    ("プログラムを機械へ渡す（カード/USB/LAN）【新】", PROGRAM_TRANSFER),
    ("パラメータ書き換え 手順（カード/USB/LAN）【新】", PARAM_REWRITE),
    ("FTP転送とDNC運転【新】", FTP_AND_DNC),
    ("実機で分かったこと（記録）【新】", TROUBLESHOOT_HISTORY),
    ("設定", SETTINGS),
    ("SwitchBotで自動測定", SWITCHBOT),
    ("ピッチエラー補正", PCORR),
    ("FANUCアラーム検索【新】", ALARM),
    ("Webアプリ連携", WEB),
    ("保存されるファイル", FILES),
]
